diff --git a/decode/chase_decoding/decoder.py b/decode/chase_decoding/decoder.py
index 698ee24..2818b2a 100644
--- a/decode/chase_decoding/decoder.py
+++ b/decode/chase_decoding/decoder.py
@@ -1,3 +1,4 @@
+from collections.abc import Mapping
 from dataclasses import dataclass
 from itertools import product
 from typing import Optional
@@ -120,7 +121,7 @@ def chase_decode(
     generator_matrix: np.ndarray,
     unreliable_positions_count: int = 2,
     inner_decoder_max_error_weight: int = 1,
-    syndrome_table: Optional[dict[tuple[int, ...], np.ndarray]] = None,
+    syndrome_table: Optional[Mapping[tuple[int, ...], np.ndarray]] = None,
 ) -> ChaseDecodingResult:
     """
     Алгоритм Чейза.
diff --git a/decode/syndrome_decoding/__init__.py b/decode/syndrome_decoding/__init__.py
index 01cf26b..75c7807 100644
--- a/decode/syndrome_decoding/__init__.py
+++ b/decode/syndrome_decoding/__init__.py
@@ -1,3 +1,8 @@
+from .compact_table import (
+    CompactSyndromeTable,
+    build_compact_syndrome_table,
+)
+
 from .decoder import (
     SyndromeDecodingResult,
     build_syndrome_table,
diff --git a/decode/syndrome_decoding/decoder.py b/decode/syndrome_decoding/decoder.py
index 6a2b2d3..caf33c5 100644
--- a/decode/syndrome_decoding/decoder.py
+++ b/decode/syndrome_decoding/decoder.py
@@ -1,3 +1,4 @@
+from collections.abc import Mapping
 from dataclasses import dataclass
 from itertools import combinations
 from typing import Optional
@@ -129,7 +130,7 @@ def syndrome_decode(
     received_word: np.ndarray,
     parity_check_matrix: np.ndarray,
     generator_matrix: Optional[np.ndarray] = None,
-    syndrome_table: Optional[dict[tuple[int, ...], np.ndarray]] = None,
+    syndrome_table: Optional[Mapping[tuple[int, ...], np.ndarray]] = None,
     max_error_weight: int = 1,
 ) -> SyndromeDecodingResult:
     """
@@ -161,7 +162,9 @@ def syndrome_decode(
 
     syndrome_key = tuple(int(value) for value in syndrome)
 
-    if syndrome_key not in syndrome_table:
+    error_vector = syndrome_table.get(syndrome_key)
+
+    if error_vector is None:
         return SyndromeDecodingResult(
             received_word=received_word,
             syndrome=syndrome,
@@ -174,8 +177,6 @@ def syndrome_decode(
                 "Ошибка не исправлена при заданном max_error_weight."
             ),
         )
-
-    error_vector = syndrome_table[syndrome_key]
     corrected_word = (received_word + error_vector) % 2
 
     check_syndrome = calculate_syndrome(
diff --git a/research/decoder_wrappers.py b/research/decoder_wrappers.py
index 0de6c7f..bd7766d 100644
--- a/research/decoder_wrappers.py
+++ b/research/decoder_wrappers.py
@@ -1,3 +1,4 @@
+from collections.abc import Mapping
 from dataclasses import dataclass
 from time import perf_counter
 from typing import Optional
@@ -70,7 +71,7 @@ def syndrome_decode_batch(
     parity_check_matrix: np.ndarray,
     generator_matrix: np.ndarray,
     max_error_weight: int,
-    syndrome_table: Optional[dict[tuple[int, ...], np.ndarray]] = None,
+    syndrome_table: Optional[Mapping[tuple[int, ...], np.ndarray]] = None,
 ) -> DecoderBatchResult:
     """
     Пакетный запуск синдромного декодера.
@@ -270,7 +271,7 @@ def chase_decode_batch(
     generator_matrix: np.ndarray,
     unreliable_positions_count: int,
     inner_decoder_max_error_weight: int,
-    syndrome_table: Optional[dict[tuple[int, ...], np.ndarray]] = None,
+    syndrome_table: Optional[Mapping[tuple[int, ...], np.ndarray]] = None,
 ) -> DecoderBatchResult:
     """
     Пакетный запуск алгоритма Чейза.
diff --git a/research/runner.py b/research/runner.py
index 4cb13ef..a990d2e 100644
--- a/research/runner.py
+++ b/research/runner.py
@@ -5,7 +5,10 @@ from typing import Any, Callable
 
 import numpy as np
 
-from decode.syndrome_decoding import build_syndrome_table
+from decode.syndrome_decoding import (
+    CompactSyndromeTable,
+    build_compact_syndrome_table,
+)
 from modulation.demodulator import (
     bpsk_llr,
     hard_decision_from_llr,
@@ -149,7 +152,7 @@ def get_chase_p_for_code(
     return decoder_config.chase_unreliable_positions_count
 
 
-SyndromeTable = dict[tuple[int, ...], np.ndarray]
+SyndromeTable = CompactSyndromeTable
 
 
 def build_syndrome_table_cache(
@@ -188,7 +191,7 @@ def build_syndrome_table_cache(
 
     for max_error_weight in sorted(required_weights):
         start_time = perf_counter()
-        tables[max_error_weight] = build_syndrome_table(
+        tables[max_error_weight] = build_compact_syndrome_table(
             parity_check_matrix=parity_check_matrix,
             max_error_weight=max_error_weight,
         )
@@ -318,6 +321,7 @@ def decoder_result_to_summary_row(
     metrics: BatchMetrics | None,
     message_count: int,
     syndrome_table_build_time_sec: float = 0.0,
+    syndrome_table: SyndromeTable | None = None,
 ) -> dict[str, Any]:
     """
     Преобразует результат декодера и метрики в строку summary.csv.
@@ -424,6 +428,18 @@ def decoder_result_to_summary_row(
         "skipped": decoder_result.skipped,
         "skip_reason": decoder_result.skip_reason or "",
         "syndrome_table_build_time_sec": syndrome_table_build_time_sec,
+        "syndrome_table_pattern_count": (
+            "" if syndrome_table is None else syndrome_table.pattern_count
+        ),
+        "syndrome_table_entry_count": (
+            "" if syndrome_table is None else syndrome_table.entry_count
+        ),
+        "syndrome_table_collision_count": (
+            "" if syndrome_table is None else syndrome_table.collision_count
+        ),
+        "syndrome_table_memory_bytes": (
+            "" if syndrome_table is None else syndrome_table.approx_memory_bytes
+        ),
     }
 
     if metrics is None:
@@ -573,9 +589,14 @@ def run_code_research(
     )
 
     for table_t, build_time in syndrome_table_build_times.items():
+        table = syndrome_tables[table_t]
+        memory_mib = table.approx_memory_bytes / (1024 ** 2)
         print(
-            f"  Таблица синдромов t={table_t}: "
-            f"{len(syndrome_tables[table_t])} синдромов, "
+            f"  Компактная таблица синдромов t={table_t}: "
+            f"{table.entry_count} синдромов / "
+            f"{table.pattern_count} шаблонов, "
+            f"коллизий {table.collision_count}, "
+            f"~{memory_mib:.2f} MiB, "
             f"построение {build_time:.3f}s"
         )
 
@@ -628,10 +649,14 @@ def run_code_research(
 
         for decoder_result in decoder_results:
             table_build_time = 0.0
+            decoder_syndrome_table: SyndromeTable | None = None
+
             if decoder_result.decoder_name == "syndrome":
                 table_build_time = syndrome_table_build_times.get(syndrome_t, 0.0)
+                decoder_syndrome_table = syndrome_tables.get(syndrome_t)
             elif decoder_result.decoder_name == "chase":
                 table_build_time = syndrome_table_build_times.get(chase_inner_t, 0.0)
+                decoder_syndrome_table = syndrome_tables.get(chase_inner_t)
             if decoder_result.skipped:
                 row = decoder_result_to_summary_row(
                     code_config=code_config,
@@ -641,6 +666,7 @@ def run_code_research(
                     metrics=None,
                     message_count=len(messages),
                     syndrome_table_build_time_sec=table_build_time,
+                    syndrome_table=decoder_syndrome_table,
                 )
                 rows.append(row)
                 print(f"    {decoder_result.decoder_name}: skipped")
@@ -678,6 +704,7 @@ def run_code_research(
                 metrics=metrics,
                 message_count=len(messages),
                 syndrome_table_build_time_sec=table_build_time,
+                syndrome_table=decoder_syndrome_table,
             )
 
             rows.append(row)
diff --git a/tests/test_research_syndrome_table_cache.py b/tests/test_research_syndrome_table_cache.py
index 635f51f..8c8edf5 100644
--- a/tests/test_research_syndrome_table_cache.py
+++ b/tests/test_research_syndrome_table_cache.py
@@ -9,11 +9,15 @@ from research.config import DecoderResearchConfig, WAVELET_16_8_CONFIG
 def test_equal_syndrome_and_chase_t_build_one_table(monkeypatch) -> None:
     calls: list[int] = []
 
-    def fake_build_syndrome_table(*, parity_check_matrix, max_error_weight):
+    def fake_build_compact_syndrome_table(*, parity_check_matrix, max_error_weight):
         calls.append(max_error_weight)
-        return {(0,): np.zeros(parity_check_matrix.shape[1], dtype=np.uint8)}
+        return object()
 
-    monkeypatch.setattr(runner, "build_syndrome_table", fake_build_syndrome_table)
+    monkeypatch.setattr(
+        runner,
+        "build_compact_syndrome_table",
+        fake_build_compact_syndrome_table,
+    )
 
     code_config = replace(
         WAVELET_16_8_CONFIG,
@@ -45,11 +49,15 @@ def test_equal_syndrome_and_chase_t_build_one_table(monkeypatch) -> None:
 def test_different_syndrome_and_chase_t_build_two_tables(monkeypatch) -> None:
     calls: list[int] = []
 
-    def fake_build_syndrome_table(*, parity_check_matrix, max_error_weight):
+    def fake_build_compact_syndrome_table(*, parity_check_matrix, max_error_weight):
         calls.append(max_error_weight)
-        return {(0,): np.zeros(parity_check_matrix.shape[1], dtype=np.uint8)}
+        return object()
 
-    monkeypatch.setattr(runner, "build_syndrome_table", fake_build_syndrome_table)
+    monkeypatch.setattr(
+        runner,
+        "build_compact_syndrome_table",
+        fake_build_compact_syndrome_table,
+    )
 
     code_config = replace(
         WAVELET_16_8_CONFIG,
diff --git a/webapp/backend/app/services/results_reader.py b/webapp/backend/app/services/results_reader.py
index 681d645..da89432 100644
--- a/webapp/backend/app/services/results_reader.py
+++ b/webapp/backend/app/services/results_reader.py
@@ -37,6 +37,10 @@ NON_METRIC_NUMERIC_COLUMNS = {
     "ambiguous_count",
     "miscorrection_count",
     "syndrome_table_build_time_sec",
+    "syndrome_table_pattern_count",
+    "syndrome_table_entry_count",
+    "syndrome_table_collision_count",
+    "syndrome_table_memory_bytes",
     "total_time_sec",
 }
 
